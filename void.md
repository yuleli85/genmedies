# 真人剧集全自动生成流水线功能设计方案 v2.0

## 一、核心设计原则

- **流式驱动**：每个模块输出即触发下游，不等全量完成再推进。
- **细粒度缓存**：每个镜头独立缓存，微调单条不影响其他镜头。
- **可中断恢复**：任意阶段中断后，已完成的镜头不重跑。
- **人工干预闭环**：每个节点可独立微调，修改后仅重跑受影响的下游。
- **质量兜底**：效果整体不达标时支持一键清空缓存全量重生成。

---

## 二、优化后流程图

```
[用户输入]
    │
    ▼
1. 剧本生成
    │  scenes + character_settings
    ├──────────────────────────────────────────────┐
    ▼                                              ▼
2. 分镜拆解（流式逐条输出）              3. 角色视觉锚点生成
    │  每条 shot 就绪即向下推送              │  character_anchors
    │                                        │  （与分镜并行，完成后注入关键帧）
    ▼                                        │
4. 关键帧生成 ←─────────────────────────────┘
    │  keyframe（逐镜头）
    ▼
5. 图生视频（逐镜头并发，最多3路）
    │  video_clip
    ▼
8. 一致性校验（逐镜头，生成完立即校验）
    │  通过 → 进入合成队列
    │  不达标 → 回退模块5重试（最多3次）
    │  超限 → 标记 manual_required，占位符入队
    ▼
    ├──────────────────────────────────────────────┐
    ▼                                              ▼
6. 多角色配音                              7. 音视频合成
   （分镜就绪后立即并行启动，               （流式合入，镜头就绪即拼接，
    不依赖视频生成结果）                     manual_required 用占位符）
    │  audio_tracks（带时间戳）                    │
    └──────────────────────────────────────────────┘
                                                   │
                                                   ▼
                                             [成片输出]
                                      （含一致性报告 + 待人工处理列表）
```

**关键并行关系：**
- 模块3（锚点）与模块2（分镜）并行，锚点完成后自动注入模块4。
- 模块6（配音）在分镜就绪后立即启动，与模块4/5/8完全并行。
- 模块7（合成）流式接收已通过校验的镜头，不等全部完成。

---

## 三、各模块详细定义

### 1. 剧本生成

- **输入**：
  - `genre`：题材（如"都市爱情"）
  - `plot_summary`：剧情梗概，≤500字
  - `episode_count`：集数，默认10
  - `duration_per_episode`：单集目标时长（秒），默认120
  - `character_settings`：角色列表（name / gender / age / appearance / personality）
  - `style`：视觉风格（如"写实电影感"）

- **输出**：
  - `script_id`
  - `scenes`：场景列表（scene_id / location / time_of_day / description / dialogues）

- **缓存策略**：
  - 以 `(genre + plot_summary + style)` 的哈希为 key 缓存剧本。
  - 缓存命中时直接复用，不重新调用 LLM。
  - 微调任意字段后，仅该集剧本失效，其他集保留。

- **微调支持**：
  - 可在编辑器中直接修改任意场景的描述或对白。
  - 修改后点击"重新生成下游"，仅重跑受影响集的模块2→8。
  - 支持单集重新生成，不影响其他集的缓存。

---

### 2. 分镜拆解

- **输入**：`script_id`（指定集）
- **约束**：
  - 单镜头时长 4~8 秒
  - 每集镜头数上限 = `duration_per_episode / 4`
  - 同一场景连续动作自动拆分为多个镜头

- **输出**（流式逐条）：
  - `shot_list`：shot_id / scene_id / shot_type / camera_move / characters / action / dialogue / duration / mood

- **缓存策略**：
  - 以 `script_id + scene_id` 为 key 逐场景缓存。
  - 修改某场景剧本，仅该场景的镜头缓存失效。

- **微调支持**：
  - 可在分镜编辑器中调整任意镜头的景别、运镜、动作描述、时长。
  - 修改单条镜头后，仅该镜头的模块4→5→8缓存失效，其他镜头不受影响。
  - 可手动插入、删除、拖拽排序镜头。

- **时长校验**：
  - `sum(shot.duration)` 与 `duration_per_episode` 偏差超过5%时，高亮提示，阻断合成。
  - 不自动缩放（真人视频变速失真），由用户手动调整镜头时长或增删镜头。

---

### 3. 角色视觉锚点生成

- **输入**：`character_settings`（剧本生成后立即可用，与分镜并行启动）

- **锚点来源**（优先级从高到低）：
  1. 用户上传真实照片（推荐，一致性最佳）
  2. 文生图模型根据 `appearance` 描述自动生成（需配合 InstantID 等 ID 保持模型）

- **输出**：
  - `character_anchors`：anchor_images（正脸 + 3/4侧脸）、face_embedding（512维特征向量）

- **缓存策略**：
  - 以 `character_id + appearance` 哈希为 key 缓存。
  - 修改角色外貌描述或替换照片后，该角色锚点缓存失效，触发所有含该角色的关键帧和视频片段重新生成。

- **微调支持**：
  - 可替换锚点图（重新上传或重新生成）。
  - 替换后系统提示受影响的镜头数量，确认后批量重跑。

- **注意**：锚点是一致性的基准，建议在流程开始前确认，修改代价较高。

---

### 4. 关键帧生成

- **输入**：单条 `shot`（流式接收）+ `character_anchors`（就绪后自动注入）
- **输出**：`keyframe`（shot_id / image_url / prompt）

- **提示词构建**：
  - 基础：shot 的 action / mood / shot_type / camera_move
  - 注入：出场角色的外貌锚点描述
  - 可选：风格参考图权重

- **质量门控**：生成后自动检测人脸数量是否与 `characters` 一致，不一致重试（最多2次）。

- **缓存策略**：
  - 以 `shot_id + prompt_hash + anchor_hash` 为 key 缓存。
  - 镜头描述或锚点变更时缓存失效，其他镜头不受影响。

- **微调支持**：
  - 可在关键帧编辑器中直接修改提示词，或手动上传替换图片。
  - 修改后仅该镜头的模块5→8缓存失效。

---

### 5. 图生视频

- **输入**：单条 `keyframe` + 对应 `shot`（action / camera_move / duration）
- **调用**：通过 `model_adapter` 接口（默认 Kling AI，可切换 Seedance / Runway Gen-2）
- **输出**：`video_clip`（shot_id / video_url / actual_duration / status）

- **并发控制**：同一集最多3路并发，避免触发 API 限流。

- **重试策略**：
  - API 失败自动重试，最多3次（与模块8触发的回退共享计数）。
  - 3次后仍失败：status = `manual_required`，黑场占位，继续流程。

- **缓存策略**：
  - 以 `shot_id + keyframe_hash + action_hash` 为 key 缓存。
  - 关键帧或动作描述变更时缓存失效。
  - 缓存命中时跳过生成，直接进入模块8校验。

- **微调支持**：
  - 可对单个镜头手动触发重新生成（不清空其他镜头缓存）。
  - 可手动上传替换视频片段，跳过生成直接进入校验。

---

### 6. 多角色配音

- **输入**：
  - `shot_list`（分镜就绪后立即启动，不等视频生成）
  - `voice_settings`（可选）：角色音色 ID / 语速 / 情绪偏好

- **输出**：
  - `audio_tracks`：每角色一轨，含 segments（shot_id / start_ms / end_ms / audio_url / emotion_tag）

- **缓存策略**：
  - 以 `shot_id + dialogue + voice_id` 哈希为 key 逐句缓存。
  - 修改台词或更换音色后，仅该句缓存失效。

- **微调支持**：
  - 可对单句重新生成配音。
  - 可调整语速、情绪标签后单句重试。
  - 可手动上传替换音频片段。

---

### 7. 音视频合成

- **输入**：
  - `video_clips`（流式接收，镜头通过校验即入队）
  - `audio_tracks`（带时间戳）
  - 可选：`bgm_url`、`sfx_list`

- **处理流程**：
  1. 按 shot_list 顺序拼接 video_clips，`manual_required` 镜头用黑场占位
  2. 混入各角色 audio_tracks，BGM 音量自动压低至对白以下
  3. 根据 dialogues 自动生成 SRT 字幕并挂载

- **输出**：`final_video`（MP4，含字幕轨）+ `consistency_report`

- **局部重合成**：
  - 人工替换某镜头后，仅重合成受影响的时间段，不重跑整集。

---

### 8. 一致性校验与自动重试

> **执行时机**：每个 video_clip 生成完立即校验，不等全集完成。

- **输入**：单条 `video_clip` + `character_anchors`
- **方法**：均匀抽帧（每秒2帧）→ 提取人脸特征向量 → 与锚点 face_embedding 计算余弦相似度 → 取均值

- **判定规则**：

  | 得分 | 处理 |
  |------|------|
  | ≥ 0.7 | 通过，进入合成队列 |
  | 0.5 ~ 0.7 | 警告，人工确认后可放行 |
  | < 0.5 | 自动回退模块5重新生成 |

- **重试上限**：每镜头最多3次（含模块5失败重试共享计数）。超限后保留最高分结果，标记 `manual_required`。

- **输出**：
  - 各镜头一致性得分
  - `consistency_report`：通过 / 警告 / 失败镜头汇总，`manual_required` 镜头列表及建议

---

## 四、缓存与重生成策略总览

### 缓存粒度

| 模块 | 缓存 key | 失效条件 |
|------|----------|----------|
| 剧本 | genre + plot_summary + style | 任意输入字段变更 |
| 分镜 | script_id + scene_id | 对应场景剧本变更 |
| 锚点 | character_id + appearance | 外貌描述变更或替换照片 |
| 关键帧 | shot_id + prompt_hash + anchor_hash | 镜头描述或锚点变更 |
| 视频片段 | shot_id + keyframe_hash + action_hash | 关键帧或动作描述变更 |
| 配音 | shot_id + dialogue + voice_id | 台词或音色变更 |

### 微调影响范围

```
修改剧本某场景  →  该场景分镜 → 关键帧 → 视频 → 校验（其他场景不受影响）
修改某镜头描述  →  该镜头关键帧 → 视频 → 校验
修改角色锚点    →  所有含该角色的关键帧 → 视频 → 校验
修改某句台词    →  该句配音（视频不受影响）
```

### 全量重生成

当整体效果不达标时（如角色一致性普遍偏低、风格偏差严重），支持：

- **清空全部缓存重跑**：一键清空所有缓存，从模块1重新开始。
- **按模块清空**：选择从某个模块开始清空，保留上游缓存。例如"从关键帧开始重跑"，保留剧本和分镜缓存。
- **按角色清空**：清空特定角色相关的所有关键帧和视频缓存，重跑该角色出现的所有镜头。

---

## 五、全流程触发逻辑

### 执行顺序

```
t=0   启动：1（剧本生成）
t=1   并行：2（分镜，流式）+ 3（锚点生成）
t=2   流式：4（关键帧，逐镜头，等锚点就绪后注入）
t=3   流式：5（图生视频，逐镜头并发）+ 6（配音，分镜就绪后立即启动）
t=4   流式：8（一致性校验，逐镜头）
t=5   流式：7（合成，镜头通过校验即入队）
```

### 错误处理

| 场景 | 处理方式 |
|------|----------|
| 模块5重试3次仍失败 | 黑场占位，标记 `manual_required`，继续流程 |
| 模块8重试3次仍 <0.5 | 保留最高分结果，标记 `manual_required` |
| 模块6配音失败 | 该段静音，标记待补录 |
| 时长偏差超5% | 阻断合成，提示用户调整分镜 |
| 锚点生成延迟（晚于首批关键帧需求） | 首批关键帧等待锚点就绪，后续镜头正常流式处理 |

---

## 六、技术指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 首轮生成可用率 | ≥40% | 无需人工修改 |
| 经自动重试后可用率 | ≥85% | 含模块8自动回退 |
| 角色一致性检测准确率 | ≥90% | 余弦相似度比对 |
| 单集生成总耗时 | ≤45分钟 | 含并行优化（原60分钟） |
| 人工干预耗时（每集） | ≤30分钟 | 仅处理 `manual_required` 镜头 |
| 缓存命中时单镜头耗时 | <1秒 | 直接复用，跳过生成 |

---

## 七、扩展与配置预留

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `model_adapter` | kling | 视频生成模型，可切换 seedance / runway |
| `consistency_threshold` | 0.7 | 一致性通过阈值 |
| `concurrent_shots` | 3 | 图生视频并发数 |
| `language` | zh | 支持 zh / en / ja |
| `style_reference` | null | 风格参考图，影响关键帧提示词 |
| `cache_ttl_days` | 7 | 缓存保留天数，过期自动清理 |

---

## 八、最小闭环原型建议

**范围**：2个角色、1个场景、3个连续镜头（合计 ≤24秒）

**验证目标**：

| 验证项 | 通过标准 | 不达标处理 |
|--------|----------|------------|
| 锚点→关键帧→视频一致性 | 均值 ≥0.7 | 引入 InstantID / IP-Adapter-FaceID |
| 一致性校验误判率 | <10% | 调整抽帧策略或相似度算法 |
| 单镜头端到端耗时 | <3分钟 | 评估并发策略或更换模型 |
| 流式驱动延迟 | 首帧 <30秒可见 | 检查模块间消息队列配置 |

**决策节点**：原型验证通过后再进入全量开发，任一项不达标先解决再推进，避免在错误的技术选型上堆功能。

---

*方案版本：v2.0 — 引入流式驱动、细粒度缓存、并行优化，单集预计耗时从60分钟降至45分钟。*
